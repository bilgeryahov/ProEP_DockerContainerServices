sudo: required

services:
  - docker

before_install:
  - docker-compose up -d

script:
  - docker run proepdockercontainerservices_proauthentication /bin/sh -c "cd /src; npm test"
  - docker run proepdockercontainerservices_webfacade /bin/sh -c "cd /src; npm test"
after_success:
  - gem install net-scp
  - gem install net-ssh
  - ruby deploy.rb